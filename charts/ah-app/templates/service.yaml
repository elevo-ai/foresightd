apiVersion: v1
kind: Service
metadata:
  name: {{ include "ah-app.fullname" . }}
  labels:
    {{- include "ah-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ah-app.selectorLabels" . | nindent 4 }}
