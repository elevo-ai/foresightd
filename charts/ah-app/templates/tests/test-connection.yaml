apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ah-app.fullname" . }}-test-connection"
  labels:
    {{- include "ah-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ah-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
